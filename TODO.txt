- PVTR support
- ParticleSystem2D: add support for bursts!
- Multitouch events!!!
- init method for world -> async buffer & texture creation w/ event before launching the world. (no startup lag anymore, "preloadable")
- option in world2d: auto dispose of textures, etc. cleanUp / destroy
- Filtering options:  nearest neighbor / linear filtering, etc...
- Tilemaps?
- texture smoothing mipmap, nearest, linear, etc. customize per object or per ND2D instance?
- premultiplied / non premultiplied alpha check for textures!
- TextureCache
- Sprite2DBatch recursive rendering (done!. need to text)
- Depricate bitmapData as constructor argument. Only Texture2D allowed! Static creation methods?
- updateFromPreset method for particlesystem to update the buffer on the fly?!
- blurmaterial
- texturerenderer -> clear optional.



